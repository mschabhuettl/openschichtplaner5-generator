'use strict';
let snapshot=null, assignments=[], jobId=null, timer=null, previous=[];
let groups=[], checkedTeams=new Set(), transposed=false, planMonth="", solving=false;
const $=id=>document.getElementById(id);
const el=(tag,text,parent)=>{const n=document.createElement(tag);if(text!==undefined)n.textContent=text;if(parent)parent.append(n);return n;};
const notice=(text,error=false)=>{$('notice').textContent=text;$('notice').classList.toggle('error',error);};
async function api(path,method='GET',data){const r=await fetch(path,{method,headers:data?{'Content-Type':'application/json'}:{},body:data?JSON.stringify(data):undefined});if(!r.ok){let e;try{e=await r.json();}catch{throw Error(`Anfrage fehlgeschlagen (HTTP ${r.status}). Bitte erneut versuchen.`);}throw Error(typeof e.detail==='string'?e.detail:JSON.stringify(e.detail??e));}return r.json();}
async function runAction(b,fn){if(b.dataset.busy==='true')return;b.dataset.busy='true';const disabled=b.disabled;b.disabled=true;b.setAttribute('aria-busy','true');updateJobButtons();try{await fn();}catch(e){notice(e.message,true);}finally{delete b.dataset.busy;b.removeAttribute('aria-busy');b.disabled=disabled;updateJobButtons();}}
function updateJobButtons(){for(const id of ['solve','recompute'])$(id).disabled=solving||!!jobId||$(id).dataset.busy==='true';$('cancel').disabled=!jobId||$('cancel').dataset.busy==='true';const switchIds=['demo','import','restore','applyJson','file'];const busy=solving||!!jobId||$('save').dataset.busy==='true'||switchIds.some(id=>$(id).dataset.busy==='true');for(const id of switchIds)$(id).disabled=busy;$('save').disabled=busy;}
function action(id,fn){$(id).onclick=()=>runAction($(id),fn);}
function field(parent,label,value,change,type='text'){const l=el('label',label,parent),i=el('input',undefined,l);i.type=type;if(type==='checkbox')i.checked=!!value;else i.value=value??'';i.onchange=()=>change(type==='checkbox'?i.checked:type==='number'?(i.value===''?null:Number(i.value)):i.value);return i;}
function select(parent,label,value,options,change){const l=el('label',label,parent),s=el('select',undefined,l);for(const [v,t] of options){const o=el('option',t,s);o.value=v;}s.value=value;s.onchange=()=>change(s.value);return s;}
function button(parent,text,fn){const b=el('button',text,parent);b.type='button';b.onclick=()=>runAction(b,fn);return b;}
function table(parent,head){parent.replaceChildren();const t=el('table',undefined,parent),tr=el('tr',undefined,el('thead',undefined,t));head.forEach(x=>{const th=el('th',x,tr);th.scope='col';});return el('tbody',undefined,t);}
async function saved(){const list=await api('/api/snapshots');$('saved').replaceChildren();list.forEach(x=>{const o=el('option',`${x.id} · Stand ${x.revision}`,$('saved'));o.value=x.id;});}
function load(s){clearTimeout(timer);timer=null;jobId=null;previous=[];$('cancel').disabled=true;$('job').textContent='';$('result').replaceChildren();$('validation').textContent='';snapshot=s;updateJobButtons();$('planView').querySelector('[value=positions]').textContent=serviceMatrix()?'Einsatzplan · Dienste':'Einsatzplan · Funktionen / Arbeitsplätze';$('start').value=s.period_start;$('end').value=s.period_end;$('timezone').value=s.timezone;$('matrixSearch').value='';if(s.metadata.history_period){$('historyStart').value=s.metadata.history_period.start;$('historyEnd').value=s.metadata.history_period.end;}else{historyDefaults();}planMonth=s.period_start.slice(0,7);assignments=structuredClone(s.assignments);$('workspace').hidden=false;render();renderPlan();notice(`Daten geladen: ${s.employees.length} Personen${s.metadata.selected_group_ids?' aus '+s.metadata.selected_group_ids.length+' ausgewählten Teams':''}. Regeln und offene Angaben prüfen.`);}
function render(){
 $('source').textContent=`Quelle: ${snapshot.source==='synthetic'?'SYNTHETISCHE DEMO':snapshot.source} · ${snapshot.period_start} bis ${snapshot.period_end} · ${snapshot.timezone} · Stand ${snapshot.revision}`;
 renderMatrix();
 let body=table($('people'),['Person','Dienstart','Bevorzugt','Sollstunden','Teams','Profile','Details']);
 snapshot.employees.forEach(e=>{const tr=el('tr',undefined,body);el('td',e.name,tr);select(el('td',undefined,tr),'Erlaubt',e.allowed_kinds.join(','),[['day','Nur Tag'],['night','Nur Nacht'],['day,night','Tag und Nacht']],v=>e.allowed_kinds=v.split(','));select(el('td',undefined,tr),'Wunsch',e.preferred_kind??'',[['','Keine Präferenz'],['day','Bevorzugt Tag'],['night','Bevorzugt Nacht']],v=>e.preferred_kind=v||null);field(el('td',undefined,tr),'Stunden',e.target_minutes/60,v=>e.target_minutes=Math.round(v*60),'number');el('td',e.team_ids.map(id=>(snapshot.metadata.group_tree??[]).find(g=>'sp5:group:'+g.id===id)?.name??id).join(', '),tr);el('td',e.profile_ids.join(', '),tr);button(el('td',undefined,tr),'Bearbeiten',()=>personDetails(e));});
 renderHistory(); renderRules(); $('json').value=JSON.stringify(snapshot,null,2);
}
function personDetails(e){const box=$('details');box.replaceChildren();el('h3',e.name,box);const grid=el('div',undefined,box);grid.className='grid';field(grid,'Beschäftigung in %',e.employment_fraction,v=>e.employment_fraction=v,'number');field(grid,'Saldo Minuten',e.balance_minutes,v=>e.balance_minutes=v,'number');field(grid,'Wochenenden erlaubt',e.allow_weekends,v=>e.allow_weekends=v,'checkbox');field(grid,'Feiertage erlaubt',e.allow_holidays,v=>e.allow_holidays=v,'checkbox');field(grid,'Profil-IDs (Komma)',e.profile_ids.join(','),v=>e.profile_ids=v.split(',').map(x=>x.trim()).filter(Boolean));
 el('h3','Freigaben',box);const approvals=el('div',undefined,box);const draw=()=>{approvals.replaceChildren();e.approvals.forEach((a,i)=>{const row=el('div',undefined,approvals);row.className='grid card';select(row,'Dienst / Arbeitsplatz',approvalPositions().find(p=>samePosition(a,p))?.id??'', [['','Position auswählen'],...approvalPositions().map(p=>[p.id,p.name])],id=>{const p=approvalPositions().find(p=>p.id===id);if(p){a.function_id=p.function_id;a.workplace_id=p.workplace_id;}});field(row,'Gültig ab',a.valid_from,v=>a.valid_from=v,'date');field(row,'Gültig bis',a.valid_until,v=>a.valid_until=v,'date');field(row,'Betreuung erforderlich',a.supervised,v=>a.supervised=v,'checkbox');button(row,'Entfernen',()=>{e.approvals.splice(i,1);draw();});});};draw();button(box,'Freigabe hinzufügen',()=>{e.approvals.push({function_id:'',workplace_id:'',valid_from:snapshot.period_start,valid_until:snapshot.period_end,supervised:false});draw();});
 el('h3','Verfügbarkeit / Teilzeit / wechselnde Wochen',box);el('p','Ein Dienst muss vollständig in erlaubte Zeitfenster passen. Wochentage: 0 = Montag bis 6 = Sonntag. Mehrere Fenster sind möglich.',box);const avail=el('div',undefined,box);const drawAvail=()=>{avail.replaceChildren();e.availability.forEach((a,i)=>{const row=el('div',undefined,avail);row.className='grid card';field(row,'Gültig ab',a.valid_from,v=>a.valid_from=v,'date');field(row,'Gültig bis',a.valid_until,v=>a.valid_until=v,'date');field(row,'Wochentage (Komma)',a.weekdays.join(','),v=>a.weekdays=v.split(',').map(Number));field(row,'Von',a.start_time,v=>a.start_time=v);field(row,'Bis (24:00 möglich)',a.end_time,v=>a.end_time=v);field(row,'Wochenzyklus',a.cycle_weeks,v=>a.cycle_weeks=v,'number');field(row,'Zyklusphase',a.cycle_phase,v=>a.cycle_phase=v,'number');field(row,'Zyklus-Startdatum',a.cycle_anchor??'',v=>a.cycle_anchor=v||null,'date');button(row,'Fenster entfernen',()=>{e.availability.splice(i,1);drawAvail();});});};drawAvail();button(box,'Zeitfenster hinzufügen',()=>{e.availability.push({valid_from:snapshot.period_start,valid_until:snapshot.period_end,weekdays:[0,1,2,3,4],start_time:'08:00',end_time:'13:00',cycle_weeks:1,cycle_phase:0,cycle_anchor:null,source:'additional'});drawAvail();});button(box,'Details schließen',()=>{box.replaceChildren();render();});}
function renderHistory(){
 const box=$('history');box.replaceChildren();const rows=snapshot.metadata.history_matrix??[];
 if(!rows.length){el('p','Keine historischen Vorschläge vorhanden.',box);return;}
 el('p',snapshot.metadata.history_notice??'Historie ist kein Qualifikationsnachweis. Vorschläge werden erst durch ausdrückliche Freigabe nutzbar.',box);
 rows.forEach(row=>{const e=snapshot.employees.find(e=>e.id===row.employee_id);if(!e)return;
 const card=el('details',undefined,box);el('summary',`${e.name}: ${row.observed_assignment_count} historische Einsätze · ${row.first_date??''} bis ${row.last_date??''}`,card);
 el('p',(row.observed_shifts??[]).map(s=>`${s.name}: ${s.count}`).join(' · '),card);
 (row.suggested_approvals??[]).forEach(a=>{const p=matrixPositions().find(p=>samePosition(a,p));if(!p)return;
 const line=el('p',`${p.name} · ${a.evidence_count} beobachtete Einsätze `,card);
 const exists=approved(e,p);const b=button(line,exists?'Im Planungszeitraum freigegeben':'Für Planungszeitraum ausdrücklich freigeben',()=>{setApproval(e,p,true);renderMatrix();renderHistory();});b.disabled=exists;});});
}
function renderRules(){let body=table($('shifts'),['Schicht','Dienstart','Zeitfenster']);snapshot.shifts.forEach(s=>{const tr=el('tr',undefined,body);el('td',s.name,tr);select(el('td',undefined,tr),'Art',s.kind,[['unconfirmed','Noch festzulegen'],['day','Tag'],['night','Nacht']],v=>s.kind=v);el('td',s.segments.map(x=>`${x.start} – ${x.end}`).join('\n'),tr);});body=table($('positions'),['Position','Funktion / Arbeitsplatz','Qualifikation']);snapshot.positions.forEach(p=>{const tr=el('tr',undefined,body);el('td',p.name,tr);el('td',`${p.name} / ${workplaceName(p.workplace_id)}`,tr);const cell=el('td',undefined,tr);field(cell,'Nachweis erforderlich',p.qualifications_required,v=>p.qualifications_required=v,'checkbox');field(cell,'Qualifikations-IDs (Komma)',p.qualification_ids.join(','),v=>p.qualification_ids=v.split(',').map(x=>x.trim()).filter(Boolean));});body=table($('demands'),['Bedarf','Schicht / Position','Minimum','Maximum','Herkunft']);snapshot.demands.forEach(d=>{const tr=el('tr',undefined,body);el('td',d.id,tr);el('td',`${snapshot.shifts.find(s=>s.id===d.shift_id)?.name??'Dienst'} / ${snapshot.positions.find(p=>p.id===d.position_id)?.name??'Position'} · ${workplaceName(snapshot.positions.find(p=>p.id===d.position_id)?.workplace_id??'')}`,tr);field(el('td',undefined,tr),'Min',d.minimum,v=>d.minimum=v,'number');field(el('td',undefined,tr),'Max',d.maximum,v=>d.maximum=v,'number');el('td',d.source,tr);});
 $('profiles').replaceChildren();snapshot.profiles.forEach(p=>{const card=el('details',undefined,$('profiles'));el('summary',`Ruheprofil ${p.id} · ${p.confirmed?'bestätigt':'unbestätigt'}`,card);const g=el('div',undefined,card);g.className='grid';field(g,'Gültig ab',p.valid_from,v=>p.valid_from=v,'date');field(g,'Gültig bis',p.valid_until,v=>p.valid_until=v,'date');[['min_rest_minutes','Ruhe Minuten'],['after_night_rest_minutes','Nach Nacht Minuten'],['after_night_block_rest_minutes','Nach Nachtblock Minuten'],['weekly_rest_minutes','Wochenruhe Minuten'],['max_consecutive_work_days','Max. aufeinanderfolgende Arbeitstage'],['max_consecutive_nights','Max. aufeinanderfolgende Nächte'],['max_weekly_minutes','Max. Wochenminuten']].forEach(([k,t])=>field(g,t,p[k],v=>p[k]=v,'number'));select(g,'Wochenruhe-Bezug',p.weekly_rest_frame,[['calendar_week','Kalenderwoche'],['rolling_elapsed','Rollierend verstrichene Zeit'],['rolling_local','Rollierend lokale Tage']],v=>p.weekly_rest_frame=v);field(g,'Tägliche Ruhe zusätzlich',p.weekly_rest_add_daily,v=>p.weekly_rest_add_daily=v,'checkbox');field(g,'Profil fachlich bestätigt',p.confirmed,v=>p.confirmed=v,'checkbox');});
 $('unresolved').replaceChildren();snapshot.unresolved.forEach((u,i)=>{const row=el('div',undefined,$('unresolved'));row.className='card';el('span',u,row);button(row,'Nach fachlicher Korrektur als geklärt markieren',()=>{snapshot.unresolved.splice(i,1);renderRules();});});if(!snapshot.unresolved.length)el('p','Keine offenen Importangaben.', $('unresolved'));$('weights').replaceChildren();Object.entries(snapshot.objectives).forEach(([k,v])=>field($('weights'),({hours:'Stunden',nights:'Nächte',weekends:'Wochenenden',holidays:'Feiertage',wishes:'Wünsche',changes:'Änderungen'})[k],v,n=>snapshot.objectives[k]=n,'number'));}
function renderPlan(){renderCalendar();const body=table($('plan'),['Person','Dienst / Position','Fixiert','Aktion']);assignments.forEach((a,i)=>{const d=snapshot.demands.find(x=>x.id===a.demand_id),s=snapshot.shifts.find(x=>x.id===d?.shift_id);const tr=el('tr',undefined,body);select(el('td',undefined,tr),'Person',a.employee_id,snapshot.employees.map(e=>[e.id,e.name]),v=>{a.employee_id=v;renderCalendar();notice('Entwurf geändert: erneut prüfen.');});el('td',`${s?.name??'Unbekannter Dienst'} · ${snapshot.positions.find(p=>p.id===d?.position_id)?.name??'Unbekannte Position'}`,tr);field(el('td',undefined,tr),'Fixieren',a.fixed,v=>{a.fixed=v;renderCalendar();},'checkbox');button(el('td',undefined,tr),'Entfernen',()=>{assignments.splice(i,1);renderPlan();});});if(!assignments.length)el('p','Noch keine Einteilungen.',$('plan'));const add=el('div',undefined,$('plan'));add.className='actions';let employee=snapshot.employees[0]?.id,demand=snapshot.demands[0]?.id;select(add,'Person hinzufügen',employee,snapshot.employees.map(e=>[e.id,e.name]),v=>employee=v);select(add,'Bedarfsposition',demand,snapshot.demands.map(d=>[d.id,`${snapshot.shifts.find(s=>s.id===d.shift_id)?.name??'Dienst'} / ${snapshot.positions.find(p=>p.id===d.position_id)?.name??'Position'}`]),v=>demand=v);button(add,'Einteilung hinzufügen',()=>{if(!employee||!demand)return;assignments.push({employee_id:employee,demand_id:demand,fixed:false,segments:[]});renderPlan();notice('Einteilung ergänzt. Vor Export oder Neuberechnung erneut prüfen.');});}
async function save(){snapshot.assignments=structuredClone(assignments);snapshot=await api('/api/snapshots','PUT',snapshot);await saved();notice('Regeln und Snapshot dauerhaft gespeichert.');}
async function solve(){if(solving||jobId)throw Error('Eine Berechnung läuft bereits.');if(!snapshot)throw Error('Zuerst Daten laden');solving=true;updateJobButtons();try{await save();const j=await api('/api/jobs','POST',{snapshot_id:snapshot.id,time_limit:Number($('limit').value),partial:$('partial').checked});jobId=j.id;updateJobButtons();$('cancel').disabled=false;clearTimeout(timer);await poll();}finally{solving=false;updateJobButtons();}}
async function poll(){const requestedJob=jobId;if(!requestedJob)return;try{const j=await api('/api/jobs/'+requestedJob);if(jobId!==requestedJob)return;$('job').textContent=`${({queued:'In Warteschlange',running:'Berechnung läuft',succeeded:'Berechnung beendet',failed:'Fehlgeschlagen',cancelled:'Abgebrochen'})[j.state]} · ${Math.max(0,Math.round((j.finished_at??Date.now()/1000)-(j.started_at??j.created_at)))} Sekunden`;if(['queued','running'].includes(j.state)){timer=setTimeout(poll,700);return;}$('cancel').disabled=true;jobId=null;updateJobButtons();if(j.error)notice(j.error);if(j.result){assignments=j.result.assignments;$('result').replaceChildren();el('p',`Solver: ${j.result.solver_status} · ${j.result.validation.complete?'Vollständig':'Nicht vollständig'} · Laufzeit ${j.result.runtime_seconds.toFixed(2)} s`,$('result'));const evaluation=el('details',undefined,$('result'));el('summary','Auswertung und offene Stellen',evaluation);el('pre',JSON.stringify({offene_Stellen:j.result.vacancies,auswertung:j.result.metrics},null,2),evaluation);$('validation').textContent=JSON.stringify(j.result.validation,null,2);if(previous.length){const old=new Set(previous.map(a=>a.employee_id+'|'+a.demand_id)),next=new Set(assignments.map(a=>a.employee_id+'|'+a.demand_id));el('p',`Vergleich: ${[...next].filter(x=>!old.has(x)).length} hinzugefügt, ${[...old].filter(x=>!next.has(x)).length} entfernt.`,$('result'));}renderPlan();}}catch(e){if(jobId!==requestedJob)return;notice(e.message,true);timer=setTimeout(poll,2000);}}
action('demo',async()=>load(await api('/api/demo')));
action('inspect',async()=>{const source=await api($('sourceType').value==='api'?'/api/remote-source':'/api/source?directory='+encodeURIComponent($('directory').value));groups=source.groups;checkedTeams.clear();renderTeams();notice(`${groups.length} Teams gefunden. Gewünschte Teams auswählen.`);});
action('import',async()=>{if(!checkedTeams.size)throw Error('Mindestens ein Team auswählen.');notice('Import einschließlich historischer Basis läuft …');const r=await api($('sourceType').value==='api'?'/api/remote-import':'/api/import','POST',{...($('sourceType').value==='directory'?{directory:$('directory').value}:{}),period_start:$('start').value,period_end:$('end').value,team_ids:[...checkedTeams],timezone:$('timezone').value,history_plan:$('historyPlan').value,history_start:$('historyStart').value||null,history_end:$('historyEnd').value||null});load(r.snapshot);});
action('save',save);action('restore',async()=>load(await api('/api/snapshots/'+encodeURIComponent($('saved').value))));action('solve',solve);action('cancel',async()=>{await api('/api/jobs/'+jobId+'/cancel','POST');await poll();});action('validate',async()=>{$('validationDetails').open=true;$('validation').textContent=JSON.stringify(await api('/api/validate','POST',{snapshot,assignments}),null,2);});action('recompute',async()=>{previous=structuredClone(assignments);snapshot.assignments=structuredClone(assignments);await solve();});action('refreshJson',()=>{$('json').value=JSON.stringify(snapshot,null,2);});action('applyJson',()=>{load(JSON.parse($('json').value));});$('file').onchange=()=>runAction($('file'),async()=>{if($('file').files[0])load(JSON.parse(await $('file').files[0].text()));});document.querySelectorAll('[data-export]').forEach(b=>b.onclick=()=>runAction(b,async()=>{const f=b.dataset.export,r=await fetch('/api/export/'+f,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({snapshot,assignments})});if(!r.ok)throw Error(JSON.stringify(await r.json()));const url=URL.createObjectURL(await r.blob()),a=el('a');a.href=url;a.download='plan.'+f;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}));saved().catch(e=>notice(e.message));

$('sourceType').onchange=()=>{$('directoryLabel').hidden=$('sourceType').value==='api';groups=[];checkedTeams.clear();renderTeams();notice('Datenquelle geändert. Teams erneut laden.');};

function renderTeams(){
 const box=$('teamTree');box.replaceChildren();
 const descendants=id=>{const found=new Set([String(id)]);let changed=true;while(changed){changed=false;groups.forEach(g=>{if(found.has(String(g.parent_id))&&!found.has(String(g.id))){found.add(String(g.id));changed=true;}});}return found;};
 groups.forEach(g=>{const row=el('label',undefined,box);row.className='team-row';row.style.paddingLeft=`${Math.min(g.depth??0,12)*18}px`;
 const input=el('input',undefined,row);input.type='checkbox';input.value=String(g.id);input.checked=checkedTeams.has(String(g.id));input.dataset.teamId=String(g.id);
 const children=[...descendants(g.id)].filter(id=>id!==String(g.id));input.indeterminate=children.length>0&&children.some(id=>checkedTeams.has(id))&&!(input.checked&&children.every(id=>checkedTeams.has(id)));
 el('span',g.name,row);input.onchange=()=>{descendants(g.id).forEach(id=>input.checked?checkedTeams.add(id):checkedTeams.delete(id));renderTeams();};
 });$('teamCount').textContent=`${checkedTeams.size} Teams ausgewählt · Nur angekreuzte Teams werden importiert`;
}
function historyDefaults(){
 const date=new Date($('start').value+'T12:00:00Z');if(Number.isNaN(+date))return;
 const before=new Date(date);before.setUTCDate(before.getUTCDate()-1);
 const from=new Date(Date.UTC(date.getUTCFullYear(),date.getUTCMonth()-3,1,12));
 from.setUTCDate(Math.min(date.getUTCDate(),new Date(Date.UTC(from.getUTCFullYear(),from.getUTCMonth()+1,0)).getUTCDate()));
 $('historyStart').value=from.toISOString().slice(0,10);$('historyEnd').value=before.toISOString().slice(0,10);
}
function serviceMatrix(){return snapshot?.metadata?.service_matrix_version===1;}
function relatedApproval(a,p){return a.function_id===p.function_id&&(serviceMatrix()&&p.workplace_id==='*'||a.workplace_id===p.workplace_id);}
function samePosition(a,b){return a.function_id===b.function_id&&a.workplace_id===b.workplace_id;}
function approved(e,p){return e.approvals.some(a=>samePosition(a,p)&&a.valid_from<=snapshot.period_start&&a.valid_until>=snapshot.period_end);}
function suggested(e,p){return (snapshot.metadata.history_matrix??[]).some(r=>r.employee_id===e.id&&(r.suggested_approvals??[]).some(a=>samePosition(a,p)));}
function dayOffset(day,delta){const date=new Date(day+'T12:00:00Z');date.setUTCDate(date.getUTCDate()+delta);return date.toISOString().slice(0,10);}
function setApproval(e,p,enabled){
 if(enabled){if(!approved(e,p))e.approvals.push({function_id:p.function_id,workplace_id:p.workplace_id,valid_from:snapshot.period_start,valid_until:snapshot.period_end,supervised:e.approvals.some(a=>relatedApproval(a,p)&&a.supervised&&a.valid_from<=snapshot.period_end&&a.valid_until>=snapshot.period_start)});}
 else {e.approvals=e.approvals.flatMap(a=>{if(!relatedApproval(a,p)||a.valid_until<snapshot.period_start||a.valid_from>snapshot.period_end)return[a];const remaining=[];if(a.valid_from<snapshot.period_start)remaining.push({...a,valid_until:dayOffset(snapshot.period_start,-1)});if(a.valid_until>snapshot.period_end)remaining.push({...a,valid_from:dayOffset(snapshot.period_end,1)});return remaining;});}
 notice('Freigabe im Planungszeitraum geändert. Qualifikationen und Freigaben außerhalb des Zeitraums bleiben erhalten. Änderungen speichern.');
}
function matrixPositions(){
 if(serviceMatrix()){const services=new Map();const add=p=>{if(!p.function_id)return;const existing=services.get(p.function_id);if(existing){existing.qualifications_required ||= p.qualifications_required;return;}const service=(snapshot.metadata.services??[]).find(s=>(s.function_id??'sp5:service:'+s.id)===p.function_id);services.set(p.function_id,{...p,id:p.function_id,workplace_id:'*',name:service?.name??p.function_name??p.name??p.function_id});};(snapshot.metadata.services??[]).forEach(add);snapshot.positions.forEach(add);snapshot.employees.flatMap(e=>e.approvals).forEach(add);(snapshot.metadata.history_matrix??[]).flatMap(r=>r.suggested_approvals??[]).forEach(add);return [...services.values()];}
 const seen=new Set(),positions=[];const add=p=>{const key=JSON.stringify([p.function_id,p.workplace_id]);if(!seen.has(key)){seen.add(key);positions.push(p);}};
 snapshot.positions.forEach(add);
 const extra=[...snapshot.employees.flatMap(e=>e.approvals),...(snapshot.metadata.history_matrix??[]).flatMap(r=>r.suggested_approvals??[])];
 extra.forEach(a=>add({id:JSON.stringify([a.function_id,a.workplace_id]),function_id:a.function_id,workplace_id:a.workplace_id,name:a.function_name&&a.workplace_name?`${a.function_name} / ${a.workplace_name}`:a.name??'Weitere Funktion / Arbeitsplatz',qualifications_required:false}));return positions;
}
function workplaceName(id){return (snapshot.metadata.workplaces??[]).find(w=>w.id===id)?.name??id;}
function approvalPositions(){const positions=matrixPositions();if(serviceMatrix())snapshot.positions.forEach(p=>positions.push({...p,name:`${p.name} · ${workplaceName(p.workplace_id)} (eingeschränkt)`}));return positions;}
function matrixVisible(){const q=$('matrixSearch').value.trim().toLocaleLowerCase();const positions=matrixPositions();if(!q)return {employees:snapshot.employees,positions};const matchingPeople=snapshot.employees.filter(e=>e.name.toLocaleLowerCase().includes(q));const matchingPositions=positions.filter(p=>p.name.toLocaleLowerCase().includes(q));return {employees:matchingPositions.length?snapshot.employees:matchingPeople,positions:matchingPeople.length?positions:matchingPositions};}
function renderMatrix(){
 $('mappingWarning').hidden=serviceMatrix()||!snapshot.source.startsWith('sp5');
 const {employees,positions}=matrixVisible(),rows=transposed?positions:employees,cols=transposed?employees:positions;
 const body=table($('matrix'),[transposed?(serviceMatrix()?'Dienst':'Funktion / Arbeitsplatz'):'Person',...cols.map(x=>x.name)]);const t=body.parentElement;t.className='matrix-table';t.setAttribute('aria-label','Freigabematrix für den Planungszeitraum');
 rows.forEach((row,ri)=>{const tr=el('tr',undefined,body);const h=el('th',undefined,tr);h.scope='row';if(!transposed)button(h,row.name,()=>personDetails(row));else h.textContent=row.name;
 cols.forEach((col,ci)=>{const e=transposed?col:row,p=transposed?row:col,allowed=approved(e,p),history=suggested(e,p);
 const covering=e.approvals.filter(a=>samePosition(a,p)&&a.valid_from<=snapshot.period_start&&a.valid_until>=snapshot.period_end);
 const supervised=allowed&&e.approvals.some(a=>relatedApproval(a,p)&&a.supervised&&a.valid_from<=snapshot.period_end&&a.valid_until>=snapshot.period_start);const partial=!allowed&&e.approvals.some(a=>relatedApproval(a,p)&&a.valid_from<=snapshot.period_end&&a.valid_until>=snapshot.period_start);
 const mixedSupervision=supervised&&e.approvals.some(a=>samePosition(a,p)&&!a.supervised&&a.valid_from<=snapshot.period_end&&a.valid_until>=snapshot.period_start);
 const workplacePartial=partial&&serviceMatrix()&&e.approvals.some(a=>relatedApproval(a,p)&&a.workplace_id!=='*'&&a.valid_from<=snapshot.period_end&&a.valid_until>=snapshot.period_start);
 const text=allowed?(mixedSupervision?'✓ Teils betreut':supervised?'✓ Betreut':'✓ Frei'):(workplacePartial?'◐ Einzelne Arbeitsplätze':partial?'◐ Teilzeitraum':history?'◇ Vorschlag':'− Keine Freigabe');const td=el('td',undefined,tr);
 const b=button(td,text,()=>{setApproval(e,p,!allowed);renderMatrix();renderHistory();$('matrix').querySelector(`[data-row="${ri}"][data-col="${ci}"]`)?.focus();});b.className='matrix-cell '+(allowed?'allowed':partial?'partial':history?'suggested':'prohibited');b.dataset.row=ri;b.dataset.col=ci;b.dataset.employeeId=e.id;b.dataset.functionId=p.function_id;b.dataset.workplaceId=p.workplace_id;b.setAttribute('aria-pressed',String(allowed));b.setAttribute('aria-label',`${e.name} · ${p.name}: ${text}. ${allowed?'Freigabe im Zeitraum entfernen':(serviceMatrix()?'Dienst an allen Arbeitsplätzen für ganzen Planungszeitraum freigeben':'Für ganzen Planungszeitraum freigeben')}`);b.title=`${snapshot.period_start} bis ${snapshot.period_end}. ${supervised?'Betreuung erforderlich. ':''}${p.qualifications_required?'Zusätzlicher Qualifikationsnachweis bleibt erforderlich.':''}`;
 b.onkeydown=event=>{const delta={ArrowRight:[0,1],ArrowLeft:[0,-1],ArrowDown:[1,0],ArrowUp:[-1,0]}[event.key];if(!delta)return;event.preventDefault();$('matrix').querySelector(`[data-row="${ri+delta[0]}"][data-col="${ci+delta[1]}"]`)?.focus();};});});
 if(!rows.length||!cols.length)el('p','Keine passenden Personen oder Dienste. Suche leeren oder Teams und Planungszeitraum beim Import prüfen.',$('matrix'));
}
let dateFormatter,timeFormatter,formatterZone;
function ensureFormatters(){if(formatterZone===snapshot.timezone)return;formatterZone=snapshot.timezone;dateFormatter=new Intl.DateTimeFormat('en-CA',{timeZone:formatterZone,year:'numeric',month:'2-digit',day:'2-digit'});timeFormatter=new Intl.DateTimeFormat('de-DE',{timeZone:formatterZone,hour:'2-digit',minute:'2-digit'});}
function localDay(value){ensureFormatters();return dateFormatter.format(new Date(value));}
function localTime(value){ensureFormatters();return timeFormatter.format(new Date(value));}
function renderCalendar(){
 if(!snapshot)return;const start=planMonth+'-01',next=new Date(start+'T12:00:00Z');next.setUTCMonth(next.getUTCMonth()+1);const last=dayOffset(next.toISOString().slice(0,10),-1);const days=[];
 for(let day=start;day<=last;day=dayOffset(day,1))days.push(day);
 $('planMonth').textContent=new Intl.DateTimeFormat('de-DE',{month:'long',year:'numeric',timeZone:'UTC'}).format(new Date(start+'T12:00:00Z'));
 $('prevMonth').disabled=planMonth<=snapshot.period_start.slice(0,7);$('nextMonth').disabled=planMonth>=snapshot.period_end.slice(0,7);
 const byPosition=$('planView').value==='positions';const rows=byPosition?matrixPositions():snapshot.employees;
 const body=table($('calendar'),[byPosition?(serviceMatrix()?'Dienst':'Funktion / Arbeitsplatz'):'Person',...days.map(day=>new Intl.DateTimeFormat('de-DE',{weekday:'short',day:'2-digit',timeZone:'UTC'}).format(new Date(day+'T12:00:00Z')))]);body.parentElement.className='calendar-table';
 [...body.parentElement.querySelectorAll('thead th')].slice(1).forEach((th,i)=>{th.scope='col';if([0,6].includes(new Date(days[i]+'T12:00:00Z').getUTCDay()))th.classList.add('weekend');});
 const entries=assignments.map((a,index)=>{const demand=snapshot.demands.find(d=>d.id===a.demand_id),shift=snapshot.shifts.find(s=>s.id===demand?.shift_id),position=snapshot.positions.find(p=>p.id===demand?.position_id),employee=snapshot.employees.find(e=>e.id===a.employee_id);return {a,index,shift,position,employee,segments:a.segments?.length?a.segments:shift?.segments??[]};});
 const indexed=new Map();const rowKey=row=>byPosition?JSON.stringify([row.function_id,serviceMatrix()?'*':row.workplace_id]):row.id;
 entries.forEach(x=>{const key=byPosition?(x.position?rowKey(x.position):null):x.a.employee_id;if(key===null)return;
 const touched=new Map();x.segments.forEach(s=>{const first=localDay(s.start),end=localDay(new Date(new Date(s.end).getTime()-1)),time=`${localTime(s.start)}–${localTime(s.end)}${first!==end?' ↪':''}`;for(let day=first<start?start:first;day<=end&&day<=last;day=dayOffset(day,1)){if(!touched.has(day))touched.set(day,[]);touched.get(day).push(time);}});
 touched.forEach((times,day)=>{const index=JSON.stringify([key,day]);if(!indexed.has(index))indexed.set(index,[]);indexed.get(index).push({...x,timeLabel:times.join(' / ')});});});
 rows.forEach(row=>{const tr=el('tr',undefined,body);const th=el('th',row.name,tr);th.scope='row';days.forEach(day=>{const td=el('td',undefined,tr);td.dataset.date=day;if([0,6].includes(new Date(day+'T12:00:00Z').getUTCDay()))td.classList.add('weekend');if(day<snapshot.period_start||day>snapshot.period_end)td.classList.add('outside-period');
 (indexed.get(JSON.stringify([rowKey(row),day]))??[]).forEach(x=>{
 const badge=button(td,`${x.a.fixed?'◆ ':''}${byPosition?(x.employee?.name??'Unbekannte Person'):(x.shift?.name??'Dienst')}\n${x.timeLabel}${!byPosition?'\n'+(x.position?.name??''):''}`,()=>{$('assignmentDetails').open=true;$('plan').querySelectorAll('tbody tr')[x.index]?.scrollIntoView({block:'center',behavior:'smooth'});});badge.className='shift-badge '+(x.shift?.kind==='night'?'night':'day');badge.title=`${x.employee?.name??''} · ${x.position?.name??''}${x.a.fixed?' · Fixiert':''}`;
 });});});
 if(!assignments.length)el('p','Noch keine Einteilungen. Berechnen oder Einteilungen im Detailbereich ergänzen.',$('calendar'));
}
$('matrixSearch').oninput=()=>snapshot&&renderMatrix();
action('transpose',()=>{transposed=!transposed;$('transpose').setAttribute('aria-pressed',String(transposed));renderMatrix();});
action('confirmHistory',()=>{const {employees,positions}=matrixVisible();const pairs=employees.flatMap(e=>positions.filter(p=>suggested(e,p)&&!approved(e,p)).map(p=>[e,p]));if(!pairs.length){notice('Keine unbestätigten historischen Vorschläge in der aktuellen Ansicht.');return;}if(!window.confirm(`${pairs.length} sichtbare historische Vorschläge ausdrücklich für ${snapshot.period_start} bis ${snapshot.period_end} freigeben? ${serviceMatrix()?'Die Freigabe gilt für den Dienst an allen Arbeitsplätzen. ':''}Qualifikationen werden dadurch nicht bestätigt.`))return;pairs.forEach(([e,p])=>setApproval(e,p,true));renderMatrix();renderHistory();});
$('start').addEventListener('change',historyDefaults);historyDefaults();
$('planView').onchange=renderCalendar;
for(const [id,delta] of [['prevMonth',-1],['nextMonth',1]])action(id,()=>{const date=new Date(planMonth+'-01T12:00:00Z');date.setUTCMonth(date.getUTCMonth()+delta);planMonth=date.toISOString().slice(0,7);renderCalendar();});

api('/api/version').then(v=>{$('version').textContent='Version '+v.version;}).catch(()=>{$('version').textContent='Version nicht verfügbar';});
